---

language: python
python:
  - 3.6
services: docker

env:
  global:
    - ROLE_NAME: driver-gpu-nvidia
  matrix:
    - MOLECULE_DISTRO: centos7
    - MOLECULE_DISTRO: ubuntu1804
    - MOLECULE_DISTRO: ubuntu1810

install:
  - pip install molecule docker

before_script:
  - cd ../
  - mv Provisioning---Ansible-role-$ROLE_NAME $ROLE_NAME
  - cd $ROLE_NAME

script:
  - molecule test

notifications:
